name: Mirror to GitLab

on:
  push:
    branches: ["**"]
    tags: ["**"]
  delete:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror clone and push
        env:
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          GITLAB_REPO:  ${{ secrets.GITLAB_REPO }}
          GH_REPO:      ${{ github.repository }}
        run: |
          set -euo pipefail

          rm -rf repo.git

          # clone แบบ mirror (bare) => ไม่มี branch ที่ checkout อยู่
          git clone --mirror "https://github.com/${GH_REPO}.git" repo.git
          cd repo.git

          # push mirror ไป GitLab
          TARGET="${GITLAB_REPO/https:\/\//https:\/\/oauth2:${GITLAB_TOKEN}@}"
          git push --mirror "$TARGET"
